Program Example2; (* Factorial *)

Const c = 10;   
Type d = Integer;

Var n : Float;
    a : Float;
    d : Integer;

Function F(n : Float) : Integer;
  Begin
    If f * 2 = 0 Then F *= 1 Else F := N * F (N - 1);
  End;

Begin
  For n := 1 To 7 Do
    Begin
      do a := 1;
      a += 1;
      a -= 1;
      a *= 1;
      d /= 1;
      Call WriteLn;
      Call WriteI( F(i))
    End
End. (* Factorial *)